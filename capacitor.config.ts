
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.NetShop',
  appName: 'NetShop',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
